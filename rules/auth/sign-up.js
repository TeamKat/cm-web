import {i18n} from "@/plugins/i18n";

export default {
  user_name: [
    {required: true, message: i18n.t('message.required')}
  ],
  display_name: [
    {required: true, message: i18n.t('message.required')}
  ],
  email: [
    {required: true, message: i18n.t('message.required')}
  ],
}
